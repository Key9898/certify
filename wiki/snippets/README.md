# Reusable Code Snippets

This directory preserves copy-paste helper layouts and template snippets.
All files must use `.md` format (markdown wrapper around code blocks) to keep frontmatter parsing correct.
